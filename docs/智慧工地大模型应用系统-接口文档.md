# 智慧工地大模型应用系统-接口文档

## 1. 文档说明

本文档描述智慧工地大模型应用系统的后端接口设计，接口内容覆盖系统探活、认证权限、项目管理、文件管理、知识库管理、知识问答、合规审查、报告生成、OCR识别、任务管理和审计日志。

| 状态 | 说明 |
| --- | --- |
| 规划中 | 当前为接口设计，需后续开发实现 |
| 管理端 | 面向平台管理员、项目管理员或运维人员 |
| 业务端 | 面向业务人员、审核人员和项目成员 |
| 内部 | 主要由后端模块、Worker或系统任务调用 |


### 1.1 技术实现说明

- 后端接口由Java + Spring Boot主系统提供。
- 前端由Vue 3 + TypeScript实现，通过REST API调用后端接口。
- 智能算法服务由Python实现，包括大模型调用、RAG检索、OCR识别、文档解析和向量化等能力。
- 前端不直接调用Python算法服务、数据库、对象存储或向量库，所有访问统一经过Java后端接口。
- Java后端与Python算法服务之间通过REST API或内部服务接口集成。

## 2. 通用约定

### 2.1 请求协议

- 协议：HTTP / HTTPS。
- 数据格式：JSON。
- 字符集：UTF-8。
- 文件上传：multipart/form-data。
- 默认服务地址：http://localhost:8080。
- API前缀：/api。

### 2.2 通用请求头

| Header | 必填 | 示例 | 说明 |
| --- | --- | --- | --- |
| Authorization | 是 | Bearer eyJ... | 登录后获取的访问令牌，公开接口除外 |
| Content-Type | 是 | application/json | JSON接口使用 |
| X-Request-Id | 否 | REQ202607020001 | 请求链路ID，不传由后端生成 |
| Idempotency-Key | 部分接口 | submit-20260702-0001 | 写操作幂等键，生成、重试、提交类接口建议必传 |

### 2.3 统一响应结构

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "requestId": "REQ202607020001",
  "timestamp": "2026-07-02T10:00:00+08:00"
}
```

### 2.4 分页响应结构

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "pageNo": 1,
    "pageSize": 20,
    "total": 100,
    "records": []
  }
}
```

### 2.5 通用错误码

| code | HTTP状态码 | 含义 | 说明 |
| --- | --- | --- | --- |
| 0 | 200 | 成功 | 请求处理成功 |
| 40000 | 400 | 参数错误 | 参数缺失、格式错误、校验失败 |
| 40100 | 401 | 未认证 | 未登录或Token无效 |
| 40300 | 403 | 无权限 | 当前用户无权访问资源 |
| 40400 | 404 | 资源不存在 | 数据不存在或已删除 |
| 40900 | 409 | 状态冲突 | 当前状态不允许该操作 |
| 42900 | 429 | 请求过频 | 触发限流或重复提交 |
| 50000 | 500 | 系统异常 | 未预期的服务端异常 |
| 50200 | 502 | 外部服务异常 | 大模型、知识库、OCR、对象存储等异常 |
| 50300 | 503 | 服务不可用 | 依赖服务不可用或系统维护 |

## 3. 枚举定义

### 3.1 文件业务类型 bizType

| 值 | 说明 |
| --- | --- |
| KNOWLEDGE_DOC | 知识库文档 |
| REVIEW_DOC | 合规审查文档 |
| REPORT_TEMPLATE | 报告模板 |
| REVIEW_TEMPLATE | 审查模板 |
| REPORT_OUTPUT | 报告生成结果 |
| OCR_INPUT | OCR输入文件 |

### 3.2 任务状态 taskStatus

| 值 | 说明 |
| --- | --- |
| PENDING | 待执行 |
| QUEUED | 已入队 |
| RUNNING | 执行中 |
| SUCCESS | 执行成功 |
| FAILED | 执行失败 |
| RETRYING | 重试中 |
| CANCELED | 已取消 |

### 3.3 任务阶段 stageCode

| 值 | 说明 |
| --- | --- |
| INIT | 初始化 |
| FILE_PARSING | 文件解析 |
| DATA_CLEANING | 数据清洗 |
| KNOWLEDGE_INDEXING | 知识库入库 |
| KNOWLEDGE_RETRIEVAL | 知识库检索 |
| DATABASE_QUERY | 数据库查询 |
| MODEL_GENERATION | 模型生成 |
| COMPLIANCE_REVIEW | 合规审查 |
| OCR_RECOGNITION | OCR识别 |
| REPORT_RENDERING | 报告渲染 |
| FINISH | 完成 |

### 3.4 报告状态 reportStatus

| 值 | 说明 |
| --- | --- |
| DRAFT | 草稿 |
| GENERATING | 生成中 |
| COMPLETED | 已完成 |
| FAILED | 生成失败 |
| ARCHIVED | 已归档 |
| DELETED | 已删除 |

## 4. 系统接口

### 4.1 系统探活

- Method：GET
- URL：/api/system/ping
- 状态：规划中
- 说明：用于检查应用服务是否可用。

#### 响应示例

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "UP",
    "service": "smart-worksite-ai",
    "time": "2026-07-02T10:00:00+08:00"
  }
}
```


### 4.2 系统配置接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/system/configs` | 查询系统配置 | 规划中 |
| PUT | `/api/system/configs/{configKey}` | 更新系统配置 | 规划中 |

### 4.3 系统运行信息接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/system/version` | 查询系统版本信息 | 规划中 |
| GET | `/api/system/runtime` | 查询运行环境信息 | 规划中 |
| GET | `/api/system/dependencies/health` | 查询依赖服务健康状态 | 规划中 |

### 4.4 系统公告与维护接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/system/notices` | 查询系统公告 | 规划中 |
| POST | `/api/system/notices` | 创建系统公告 | 规划中 |
| PUT | `/api/system/maintenance-mode` | 更新维护模式 | 规划中 |
## 5. 认证与权限接口

### 5.1 登录

- Method：POST
- URL：/api/auth/login
- Local dev default administrator: `admin / admin123`; production deployments must reset this password.
- 状态：规划中
- 说明：用户登录并获取访问令牌。

#### 请求体

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

#### 响应data

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| accessToken | string | 访问令牌 |
| expiresIn | integer | 有效期秒数 |
| userId | long | 用户ID |
| displayName | string | 用户显示名称 |

### 5.2 获取当前用户信息

- Method：GET
- URL：/api/auth/me
- 状态：规划中

### 5.3 查询用户项目权限

- Method：GET
- URL：/api/auth/projects/{projectId}/permissions
- 状态：规划中


### 5.4 退出登录

- Method：POST
- URL：/api/auth/logout
- 状态：规划中
- 说明：退出登录并使当前令牌失效。

### 5.5 刷新访问令牌

- Method：POST
- URL：/api/auth/token/refresh
- 状态：规划中

### 5.6 修改当前用户密码

- Method：PUT
- URL：/api/auth/password
- 状态：规划中

### 5.7 登录会话接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/auth/logout` | 退出登录并使当前令牌失效 | 规划中 |
| POST | `/api/auth/token/refresh` | 刷新访问令牌 | 规划中 |
| PUT | `/api/auth/password` | 修改当前用户密码 | 规划中 |

### 5.8 用户管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/users` | 分页查询用户 | 规划中 |
| POST | `/api/users` | 创建用户 | 规划中 |
| GET | `/api/users/{userId}` | 查询用户详情 | 规划中 |
| PUT | `/api/users/{userId}` | 编辑用户 | 规划中 |
| POST | `/api/users/{userId}/enable` | 启用用户 | 规划中 |
| POST | `/api/users/{userId}/disable` | 禁用用户 | 规划中 |
| POST | `/api/users/{userId}/reset-password` | 重置密码 | 规划中 |
| POST | `/api/users/import` | 批量导入用户 | 规划中 |
| POST | `/api/users/batch-disable` | 批量禁用用户 | 规划中 |

### 5.9 角色管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/roles` | 分页查询角色 | 规划中 |
| POST | `/api/roles` | 创建角色 | 规划中 |
| GET | `/api/roles/{roleId}` | 查询角色详情 | 规划中 |
| PUT | `/api/roles/{roleId}` | 编辑角色 | 规划中 |
| DELETE | `/api/roles/{roleId}` | 删除角色 | 规划中 |

### 5.10 权限配置接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/permissions` | 查询权限树 | 规划中 |
| PUT | `/api/roles/{roleId}/permissions` | 绑定角色权限 | 规划中 |
| PUT | `/api/users/{userId}/roles` | 分配用户平台角色 | 规划中 |
| GET | `/api/auth/menus` | 查询当前用户菜单 | 规划中 |
| GET | `/api/auth/buttons` | 查询当前用户按钮权限 | 规划中 |
## 6. 项目管理接口

### 6.1 创建项目

- Method：POST
- URL：/api/projects
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectName | string | 是 | 工地项目名称 |
| projectCode | string | 是 | 项目编码 |
| location | string | 否 | 项目地址 |
| description | string | 否 | 项目说明 |

### 6.2 查询项目列表

- Method：GET
- URL：/api/projects
- 状态：规划中

| Query参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | string | 否 | 项目名称或编码 |
| pageNo | integer | 否 | 页码 |
| pageSize | integer | 否 | 每页数量 |

### 6.3 查询项目详情

- Method：GET
- URL：/api/projects/{projectId}
- 状态：规划中

### 6.4 更新项目

- Method：PUT
- URL：/api/projects/{projectId}
- 状态：规划中


### 6.5 项目状态接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| DELETE | `/api/projects/{projectId}` | 逻辑删除项目 | 规划中 |
| POST | `/api/projects/{projectId}/enable` | 启用项目 | 规划中 |
| POST | `/api/projects/{projectId}/disable` | 停用项目 | 规划中 |
| POST | `/api/projects/{projectId}/archive` | 归档项目 | 规划中 |

### 6.6 项目成员接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/projects/{projectId}/members` | 查询项目成员 | 规划中 |
| POST | `/api/projects/{projectId}/members` | 添加项目成员 | 规划中 |
| PUT | `/api/projects/{projectId}/members/{memberId}` | 修改成员角色 | 规划中 |
| DELETE | `/api/projects/{projectId}/members/{memberId}` | 移除项目成员 | 规划中 |

### 6.7 项目配置与统计接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/projects/{projectId}/settings` | 查询项目配置 | 规划中 |
| PUT | `/api/projects/{projectId}/settings` | 更新项目配置 | 规划中 |
| GET | `/api/projects/{projectId}/statistics` | 查询项目统计 | 规划中 |
| GET | `/api/projects/{projectId}/recent-activities` | 查询最近动态 | 规划中 |
## 7. 文件管理接口

### 7.1 上传文件

- Method：POST
- URL：/api/files
- Body类型：multipart/form-data
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | File | 是 | 上传文件 |
| projectId | long | 是 | 项目ID |
| bizType | string | 是 | 文件业务类型 |
| bizId | long | 否 | 关联业务ID |

#### 响应data

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| fileId | long | 文件ID |
| fileName | string | 原始文件名 |
| objectName | string | 对象存储路径 |
| contentType | string | 文件类型 |
| fileSize | long | 文件大小 |

### 7.2 查询文件详情

- Method：GET
- URL：/api/files/{fileId}
- 状态：规划中

### 7.3 获取文件访问URL

- Method：GET
- URL：/api/files/{fileId}/access-url
- 状态：规划中

| Query参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| usage | string | 是 | DOWNLOAD或PREVIEW |
| expireSeconds | integer | 否 | 有效期，默认600秒 |

### 7.4 删除文件

- Method：DELETE
- URL：/api/files/{fileId}
- 状态：规划中


### 7.5 文件访问兼容接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/files/upload` | 文件上传兼容接口，等价于`POST /api/files` | 规划中 |
| GET | `/api/files/{fileId}/download-url` | 获取下载URL兼容接口 | 规划中 |
| GET | `/api/files/{fileId}/preview-url` | 获取预览URL兼容接口 | 规划中 |

### 7.6 文件引用与版本接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/files/{fileId}/references` | 查询文件业务引用 | 规划中 |
| GET | `/api/files/{fileId}/versions` | 查询文件版本 | 规划中 |
| POST | `/api/files/check-duplicate` | 上传前重复检测 | 规划中 |

### 7.7 文件清洗接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/file-parse-records/{recordId}/clean` | 触发解析结果清洗 | 规划中 |
| GET | `/api/file-clean-records/{recordId}` | 查询清洗记录 | 规划中 |
| GET | `/api/file-clean-records/{recordId}/diff` | 查询清洗前后对比 | 规划中 |
## 8. 知识库接口

### 8.1 创建知识库

- Method：POST
- URL：/api/projects/{projectId}/knowledge-bases
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 知识库名称 |
| description | string | 否 | 知识库说明 |
| domain | string | 否 | 知识库领域 |

### 8.2 上传知识库文档

- Method：POST
- URL：/api/knowledge-bases/{knowledgeBaseId}/documents
- Body类型：multipart/form-data
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | File | 是 | 知识库文档 |
| sourceType | string | 否 | 来源类型 |

### 8.3 查询知识库文档列表

- Method：GET
- URL：/api/knowledge-bases/{knowledgeBaseId}/documents
- 状态：规划中

### 8.4 触发知识库入库任务

- Method：POST
- URL：/api/knowledge-documents/{documentId}/index
- 状态：规划中

### 8.5 知识库检索

- Method：POST
- URL：/api/knowledge/search
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| query | string | 是 | 检索问题 |
| topK | integer | 否 | 返回数量 |
| domain | string | 否 | 领域过滤 |


### 8.6 知识库基础管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/knowledge-bases/{knowledgeBaseId}` | 查询知识库详情 | 规划中 |
| PUT | `/api/knowledge-bases/{knowledgeBaseId}` | 编辑知识库 | 规划中 |
| DELETE | `/api/knowledge-bases/{knowledgeBaseId}` | 删除知识库 | 规划中 |
| POST | `/api/knowledge-bases/{knowledgeBaseId}/enable` | 启用知识库 | 规划中 |
| POST | `/api/knowledge-bases/{knowledgeBaseId}/disable` | 停用知识库 | 规划中 |

### 8.7 知识库文档接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/knowledge-documents/{documentId}` | 查询知识库文档详情 | 规划中 |
| DELETE | `/api/knowledge-documents/{documentId}` | 删除知识库文档 | 规划中 |
| GET | `/api/knowledge-documents/{documentId}/chunks` | 查询文档切片 | 规划中 |
| GET | `/api/knowledge-documents/{documentId}/versions` | 查询文档版本 | 规划中 |

### 8.8 知识库入库任务接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/knowledge/index-tasks` | 批量创建入库任务 | 规划中 |
| GET | `/api/knowledge/index-tasks/{taskId}` | 查询入库任务 | 规划中 |
## 9. 知识问答接口

### 9.1 创建问答会话

- Method：POST
- URL：/api/qa/sessions
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| title | string | 否 | 会话标题 |

### 9.2 提交问题

- Method：POST
- URL：/api/qa/sessions/{sessionId}/messages
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| question | string | 是 | 用户问题 |
| routeMode | string | 否 | AUTO、MODEL、KNOWLEDGE、DATABASE、MIXED |
| dataSourceIds | array | 否 | 可用数据源ID |
| knowledgeBaseIds | array | 否 | 可用知识库ID |

#### 响应data

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| messageId | long | 消息ID |
| answer | string | 回答内容 |
| routeMode | string | 实际路由方式 |
| references | array | 引用来源 |
| needClarification | boolean | 是否需要用户补充信息 |
| clarificationQuestions | array | 主动追问列表 |

### 9.3 查询会话消息

- Method：GET
- URL：/api/qa/sessions/{sessionId}/messages
- 状态：规划中

### 9.4 提交回答反馈

- Method：POST
- URL：/api/qa/messages/{messageId}/feedback
- 状态：规划中


### 9.5 会话管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/qa/sessions` | 查询问答会话列表 | 规划中 |
| GET | `/api/qa/sessions/{sessionId}` | 查询会话详情 | 规划中 |
| PUT | `/api/qa/sessions/{sessionId}` | 修改会话标题 | 规划中 |
| DELETE | `/api/qa/sessions/{sessionId}` | 删除或归档会话 | 规划中 |

### 9.6 消息与引用接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/qa/sessions/{sessionId}/messages/{messageId}/regenerate` | 重新生成回答 | 规划中 |
| GET | `/api/qa/messages/{messageId}` | 查询消息详情 | 规划中 |
| GET | `/api/qa/messages/{messageId}/references` | 查询回答引用来源 | 规划中 |
## 10. 合规审查接口

### 10.1 上传审查模板

- Method：POST
- URL：/api/review/templates
- Body类型：multipart/form-data
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | File | 是 | 模板文件 |
| projectId | long | 是 | 项目ID |
| templateName | string | 是 | 模板名称 |
| templateType | string | 是 | 模板类型 |

### 10.2 查询审查模板列表

- Method：GET
- URL：/api/review/templates
- 状态：规划中

### 10.3 提交文档审查

- Method：POST
- URL：/api/review/records
- Body类型：multipart/form-data
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| templateId | long | 是 | 审查模板ID |
| file | File | 是 | 待审查文档 |

### 10.4 查询审查结果

- Method：GET
- URL：/api/review/records/{recordId}
- 状态：规划中

#### 审查结果示例

```json
{
  "recordId": 10001,
  "status": "COMPLETED",
  "issues": [
    {
      "issueId": "ISSUE-001",
      "severity": "HIGH",
      "location": "第3页 第2段",
      "ruleName": "安全措施完整性要求",
      "description": "缺少临边防护措施说明",
      "suggestion": "补充临边防护设置、验收责任人和检查频次。"
    }
  ]
}
```


### 10.5 审查记录管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/review/records` | 分页查询审查记录 | 规划中 |
| POST | `/api/review/records/{recordId}/retry` | 重试审查 | 规划中 |
| DELETE | `/api/review/records/{recordId}` | 删除审查记录 | 规划中 |
| POST | `/api/review/records/{recordId}/archive` | 归档审查记录 | 规划中 |

### 10.6 审查问题与结果接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| PUT | `/api/review/records/{recordId}/issues/{issueId}` | 更新问题处理状态 | 规划中 |
| GET | `/api/review/records/{recordId}/download` | 下载审查结果 | 规划中 |

### 10.7 审查模板管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/review/templates/{templateId}` | 查询审查模板详情 | 规划中 |
| PUT | `/api/review/templates/{templateId}` | 编辑审查模板 | 规划中 |
| DELETE | `/api/review/templates/{templateId}` | 删除审查模板 | 规划中 |
| POST | `/api/review/templates/{templateId}/enable` | 启用审查模板 | 规划中 |
| POST | `/api/review/templates/{templateId}/disable` | 停用审查模板 | 规划中 |
## 11. 报告生成接口

### 11.1 上传报告模板

- Method：POST
- URL：/api/report/templates
- Body类型：multipart/form-data
- 状态：规划中

### 11.2 查询报告模板变量

- Method：GET
- URL：/api/report/templates/{templateId}/variables
- 状态：规划中

### 11.3 创建报告生成任务

- Method：POST
- URL：/api/reports
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| reportType | string | 是 | 报告类型 |
| templateId | long | 是 | 报告模板ID |
| knowledgeBaseIds | array | 否 | 知识库ID列表 |
| dataSourceIds | array | 否 | 数据源ID列表 |
| referenceFileIds | array | 否 | 参考文件ID列表 |
| variables | object | 否 | 模板变量值 |

#### 响应data

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| reportId | long | 报告ID |
| taskId | long | 生成任务ID |
| status | string | 报告状态 |

### 11.4 查询报告列表

- Method：GET
- URL：/api/reports
- 状态：规划中

### 11.5 查询报告详情

- Method：GET
- URL：/api/reports/{reportId}
- 状态：规划中

### 11.6 重新生成报告

- Method：POST
- URL：/api/reports/{reportId}/regenerate
- 状态：规划中

### 11.7 下载报告

- Method：GET
- URL：/api/reports/{reportId}/download
- 状态：规划中

| Query参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| format | string | 是 | WORD或PDF |


### 11.8 报告版本接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/reports/{reportId}/versions` | 查询报告版本列表 | 规划中 |
| GET | `/api/reports/{reportId}/versions/{versionId}` | 查询报告版本详情 | 规划中 |

### 11.9 报告预览、归档与分享接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/reports/{reportId}/preview` | 查询报告预览URL或预览内容 | 规划中 |
| POST | `/api/reports/{reportId}/archive` | 归档报告 | 规划中 |
| DELETE | `/api/reports/{reportId}` | 删除报告 | 规划中 |
| POST | `/api/reports/{reportId}/share` | 创建报告分享链接 | 规划中 |
| DELETE | `/api/reports/{reportId}/share/{shareId}` | 撤销报告分享 | 规划中 |

### 11.10 报告模板管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/report/templates/{templateId}` | 查询报告模板详情 | 规划中 |
| PUT | `/api/report/templates/{templateId}` | 编辑报告模板 | 规划中 |
| DELETE | `/api/report/templates/{templateId}` | 删除报告模板 | 规划中 |
| POST | `/api/report/templates/{templateId}/enable` | 启用报告模板 | 规划中 |
| POST | `/api/report/templates/{templateId}/disable` | 停用报告模板 | 规划中 |
## 12. OCR识别接口

### 12.1 提交OCR识别

- Method：POST
- URL：/api/ocr/records
- Body类型：multipart/form-data
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| ocrType | string | 是 | ID_CARD、LICENSE_PLATE、INVOICE、CONTRACT、CUSTOM |
| file | File | 是 | 待识别文件 |
| customFields | string | 否 | 自定义字段JSON字符串 |

### 12.2 查询OCR结果

- Method：GET
- URL：/api/ocr/records/{recordId}
- 状态：规划中

#### 响应data示例

```json
{
  "recordId": 30001,
  "ocrType": "CONTRACT",
  "status": "COMPLETED",
  "fields": [
    {
      "fieldName": "甲方",
      "fieldValue": "某建设单位",
      "confidence": 0.98,
      "location": "第1页"
    },
    {
      "fieldName": "合同金额",
      "fieldValue": "1200000.00",
      "confidence": 0.96,
      "location": "第2页"
    }
  ]
}
```

### 12.3 更新OCR修订结果

- Method：PUT
- URL：/api/ocr/records/{recordId}/fields
- 状态：规划中


### 12.4 OCR记录接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/ocr/records` | 分页查询OCR记录 | 规划中 |
| POST | `/api/ocr/records/{recordId}/retry` | 重试OCR识别 | 规划中 |
| DELETE | `/api/ocr/records/{recordId}` | 删除OCR记录 | 规划中 |

### 12.5 OCR结果接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/ocr/records/{recordId}/download` | 下载OCR结果JSON | 规划中 |
| POST | `/api/ocr/records/{recordId}/confirm` | 确认OCR结果 | 规划中 |

### 12.6 OCR类型接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/ocr/types` | 查询支持的OCR类型和字段模板 | 规划中 |
## 13. 任务管理接口

### 13.1 查询任务详情

- Method：GET
- URL：/api/tasks/{taskId}
- 状态：规划中

### 13.2 查询任务阶段日志

- Method：GET
- URL：/api/tasks/{taskId}/stages
- 状态：规划中

### 13.3 重试任务

- Method：POST
- URL：/api/tasks/{taskId}/retry
- 状态：规划中

### 13.4 取消任务

- Method：POST
- URL：/api/tasks/{taskId}/cancel
- 状态：规划中


### 13.5 任务列表接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/tasks` | 分页查询任务列表 | 规划中 |

### 13.6 任务统计接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/tasks/statistics` | 查询任务状态统计和队列积压 | 规划中 |
## 14. 数据源接口

### 14.1 创建数据源

- Method：POST
- URL：/api/data-sources
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| name | string | 是 | 数据源名称 |
| dbType | string | 是 | MYSQL、POSTGRESQL、KINGBASE |
| jdbcUrl | string | 是 | 数据库连接地址 |
| username | string | 是 | 用户名 |
| password | string | 是 | 密码，存储前需加密 |

### 14.2 测试数据源连接

- Method：POST
- URL：/api/data-sources/{dataSourceId}/test
- 状态：规划中

### 14.3 数据库问答查询

- Method：POST
- URL：/api/database/query
- 状态：规划中

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 是 | 项目ID |
| question | string | 是 | 查询问题 |
| dataSourceId | long | 是 | 数据源ID |


### 14.4 数据源基础管理接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/data-sources` | 分页查询数据源 | 规划中 |
| GET | `/api/data-sources/{dataSourceId}` | 查询数据源详情 | 规划中 |
| PUT | `/api/data-sources/{dataSourceId}` | 编辑数据源 | 规划中 |
| DELETE | `/api/data-sources/{dataSourceId}` | 删除数据源 | 规划中 |

### 14.5 数据源状态与结构接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| POST | `/api/data-sources/{dataSourceId}/enable` | 启用数据源 | 规划中 |
| POST | `/api/data-sources/{dataSourceId}/disable` | 停用数据源 | 规划中 |
| GET | `/api/data-sources/{dataSourceId}/schema` | 查询数据源表结构摘要 | 规划中 |
## 15. 审计日志接口

### 15.1 查询审计日志

- Method：GET
- URL：/api/audit/logs
- 状态：规划中

| Query参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| projectId | long | 否 | 项目ID |
| operatorId | long | 否 | 操作人ID |
| action | string | 否 | 操作类型 |
| objectType | string | 否 | 对象类型 |
| startTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |
| pageNo | integer | 否 | 页码 |
| pageSize | integer | 否 | 每页数量 |


### 15.2 外部调用日志接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/audit/external-call-logs` | 查询外部服务调用日志 | 规划中 |

### 15.3 接口访问日志接口

| 方法 | 路径 | 说明 | 状态 |
| --- | --- | --- | --- |
| GET | `/api/audit/access-logs` | 查询接口访问日志 | 规划中 |

### 15.4 接口实现约束

- 所有写操作建议支持 `Idempotency-Key`，避免重复提交任务或重复生成报告。
- 所有项目级接口必须校验当前用户是否拥有目标项目权限。
- 文件下载、预览、报告下载、OCR结果下载必须在生成访问地址前校验数据权限。
- 长耗时接口不得阻塞HTTP请求，应返回业务ID、任务ID和初始状态，由前端轮询任务接口。
- 所有调用Python智能算法服务的接口必须记录外部调用日志，包含服务名、调用类型、请求摘要、响应摘要、耗时、状态和错误信息。
- 数据库问答接口只能执行只读SQL，必须限制最大返回行数、执行超时和敏感字段展示。
- 所有列表接口默认过滤 `deleted = 0`，项目级数据必须包含 `projectId` 查询条件或从路径参数推导项目范围。
## 16. Apipost测试建议

### 16.1 环境变量

| 变量名 | 变量值 |
| --- | --- |
| baseUrl | http://localhost:8080 |
| accessToken | 登录接口返回的accessToken |
| projectId | 测试项目ID |

### 16.2 公共请求头

| Key | Value |
| --- | --- |
| Authorization | Bearer {{accessToken}} |
| X-Request-Id | REQ-APIPOST-001 |

### 16.3 测试顺序

1. 系统探活。
2. 登录获取accessToken。
3. 创建项目。
4. 上传文件。
5. 创建知识库并上传文档。
6. 提交知识问答。
7. 上传审查模板并提交合规审查。
8. 上传报告模板并创建报告生成任务。
9. 提交OCR识别。
10. 查询任务日志和审计日志。


## 17. 前端页面接口使用清单

本节不新增后端业务能力，仅说明前端页面与既有接口的对应关系，便于前端开发和自测。

### 17.1 登录与首页

| 页面 | 使用接口 | 说明 |
| --- | --- | --- |
| 登录页 | POST /api/auth/login | 获取访问令牌 |
| 当前用户 | GET /api/auth/me | 获取用户信息、角色和默认项目 |
| 首页工作台 | GET /api/projects、GET /api/tasks/{taskId}、GET /api/reports | 展示项目、任务和最近报告 |

### 17.2 知识库管理页

| 页面能力 | 使用接口 | 说明 |
| --- | --- | --- |
| 知识库列表 | GET /api/projects/{projectId}/knowledge-bases | 查询项目知识库 |
| 创建知识库 | POST /api/projects/{projectId}/knowledge-bases | 创建项目知识库 |
| 上传文档 | POST /api/knowledge-bases/{knowledgeBaseId}/documents | 上传知识库文档 |
| 入库处理 | POST /api/knowledge-documents/{documentId}/index | 触发解析和入库任务 |
| 文档列表 | GET /api/knowledge-bases/{knowledgeBaseId}/documents | 查看处理状态 |

### 17.3 知识问答页

| 页面能力 | 使用接口 | 说明 |
| --- | --- | --- |
| 创建会话 | POST /api/qa/sessions | 新建问答会话 |
| 发送问题 | POST /api/qa/sessions/{sessionId}/messages | 提交问题并获取回答 |
| 历史消息 | GET /api/qa/sessions/{sessionId}/messages | 展示上下文 |
| 回答反馈 | POST /api/qa/messages/{messageId}/feedback | 用户反馈 |

### 17.4 合规审查页

| 页面能力 | 使用接口 | 说明 |
| --- | --- | --- |
| 上传模板 | POST /api/review/templates | 上传审查模板 |
| 模板列表 | GET /api/review/templates | 选择审查标准 |
| 提交审查 | POST /api/review/records | 上传文档并发起审查 |
| 审查结果 | GET /api/review/records/{recordId} | 展示问题和建议 |

### 17.5 报告管理页

| 页面能力 | 使用接口 | 说明 |
| --- | --- | --- |
| 上传模板 | POST /api/report/templates | 上传报告模板 |
| 变量查看 | GET /api/report/templates/{templateId}/variables | 展示模板变量 |
| 创建报告 | POST /api/reports | 创建报告生成任务 |
| 报告列表 | GET /api/reports | 分页筛选报告 |
| 报告详情 | GET /api/reports/{reportId} | 查看报告状态和版本 |
| 重新生成 | POST /api/reports/{reportId}/regenerate | 重新发起生成 |
| 下载报告 | GET /api/reports/{reportId}/download | 下载Word/PDF |

### 17.6 OCR识别页

| 页面能力 | 使用接口 | 说明 |
| --- | --- | --- |
| 提交识别 | POST /api/ocr/records | 上传文件并指定识别类型 |
| 识别结果 | GET /api/ocr/records/{recordId} | 展示字段、置信度和位置 |
| 修订结果 | PUT /api/ocr/records/{recordId}/fields | 保存人工修订结果 |

### 17.7 前端通用处理

- 所有页面请求失败时读取统一响应结构中的 `code` 和 `message`。
- 401跳转登录页，403展示无权限页面。
- 文件上传接口使用 `multipart/form-data`，不要手动写死boundary。
- 报告生成、OCR识别、知识库入库等耗时操作根据 `taskId` 查询任务详情和阶段日志。
- 前端每个页面模块由对应负责人自行完成页面自测，不设置单独统一测试负责人。


## 18. AI Adapter APIs

### 18.1 Model Invoke

`POST /api/ai/model/invoke` accepts `projectId`, `prompt`, `systemPrompt`, `modelName`, `parameters`, and `contextMessages`; returns `answer`, `usage`, and `providerTraceId`.

### 18.2 Agent Invoke

`POST /api/ai/agent/invoke` accepts `projectId`, `goal`, `tools`, `contextMessages`, and `parameters`; returns `result`, `steps`, `followUpQuestions`, and `providerTraceId`.

### 18.3 RAG Search

`POST /api/ai/knowledge/search` accepts `projectId`, `query`, `knowledgeBaseIds`, `libraryTypes`, `topK`, `scoreThreshold`, and `rerankEnabled`; returns source records, snippets, scores, and metadata.

### 18.4 Database Q&A

`POST /api/ai/database/query` accepts `projectId`, `dataSourceId`, `question`, and `context`. The Python service generates SQL and result summaries; Java validates and executes read-only queries against enabled MySQL data sources in the current project.

### 18.5 Routing And Context

`POST /api/ai/route` returns `MODEL`, `KNOWLEDGE`, `DATABASE`, `HYBRID`, or `NEED_MORE_INFO`. `POST /api/ai/context/prepare` returns compact context messages, referenced messages, missing fields, and follow-up questions.

### 18.6 External Call Logs

`GET /api/ai/external-call-logs` supports paged query by `projectId`, `serviceName`, `callType`, and `status`.


## Advanced AI Adapter Update

The AI adapter now includes document indexing, chunking, embedding, vector retrieval, reranking, and a Python tool-calling Agent framework. The Python service supports `LOCAL`, `PGVECTOR`, and `MILVUS` vector providers. Qwen embeddings are used in production through `EMBEDDING_PROVIDER=QWEN`; Qwen rerank is configured through `RERANK_PROVIDER=QWEN`, `QWEN_RERANK_BASE_URL`, and `QWEN_RERANK_MODEL`; `LOCAL_HASH` is reserved for offline tests. Java exposes `/api/ai/knowledge/index` and `/api/ai/knowledge/search`, but Java does not directly access vector databases.

Database Q&A now supports MySQL, PostgreSQL, and Kingbase JDBC drivers while preserving strict read-only SQL validation, query timeout, and maximum-row limits.


PostgreSQL and Kingbase JDBC drivers are enabled for database Q&A execution. PostgreSQL can be integration-tested with `AI_TEST_POSTGRES_JDBC_URL`; Kingbase can be integration-tested with `AI_TEST_KINGBASE_JDBC_URL` when an available Kingbase test instance exists.



